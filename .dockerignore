node_modules
**/node_modules
dist
**/dist
coverage
**/coverage
.git
.gitignore
.env
.env.*
!.env.example
uploads
# frontend/src is deliberately NOT excluded: this .dockerignore is shared by both
# images, and the frontend build needs it. The backend image simply never COPYs it,
# so it costs the backend nothing but a slightly larger build context.
*.md
.vscode
.idea
