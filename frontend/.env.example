# Vite exposes only VITE_-prefixed variables to the client bundle.
#
# In development the frontend calls a same-origin `/api` path and Vite's dev proxy
# forwards it to the backend (§16), so no base URL is needed. Set this only when the
# built frontend is served from a different origin than the API.
# VITE_API_BASE_URL=
