// Placeholder shell. MUI theme, routing and React Query arrive in Phase 8 (§19).
export default function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1 style={{ color: '#4F46E5' }}>ClearChain</h1>
      <p>Scaffold ready — UI shell arrives in Phase 8.</p>
    </main>
  );
}
